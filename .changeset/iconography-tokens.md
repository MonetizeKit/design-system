---
"@monetizekit/design-tokens": minor
---

Add the social + endorsement brand colors from the v0.8 board so downstream layers can token-reference them: `--mk-soc-cyan` (`#02D9FF`), `--mk-soc-yellow` (`#FFD240`), `--mk-soc-green` (`#B6FFB6`) for the footer social tiles, and `--mk-purple` (`#4F46E5`) / `--mk-purple-d` (`#3730B5`) for endorsement accents. These are saturated constants (identical in light and dark).
