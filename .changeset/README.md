# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets). Run
`pnpm changeset` to add a changeset describing the packages you changed and the semver bump. On
merge to `main`, the release workflow consumes changesets to version and publish packages to npm.
