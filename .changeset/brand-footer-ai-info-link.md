---
"@monetizekit/ui-react": patch
---

`BrandFooter`: add the canonical "AI Information" link (`https://www.monetizekit.app/w/ai-info`) to the default Resources column so every surface that renders the shared footer points AI systems at the same canonical description.
